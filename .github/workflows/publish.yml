name: publish
on:
  push:
    tags:
      - "v*"
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"
      - run: npm ci
      - run: npm run build
      - run: npm test
      - run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Create GitHub Release
        run: |
          # Fetch tags to ensure previous tag is available
          git fetch --tags origin 2>/dev/null || true
          # Get commits since the previous tag
          PREV_TAG=$(git tag --sort=-creatordate | sed -n '2p')
          if [ -n "$PREV_TAG" ]; then
            NOTES=$(git log "$PREV_TAG"..HEAD --pretty=format:'- %s' --no-merges)
          else
            NOTES=$(git log --pretty=format:'- %s' --no-merges $(git rev-list --max-parents=0 HEAD)..HEAD)
          fi
          # Create or update release
          if gh release view "${{ github.ref_name }}" &>/dev/null; then
            gh release edit "${{ github.ref_name }}" --notes "$NOTES"
          else
            gh release create "${{ github.ref_name }}" --title "${{ github.ref_name }}" --notes "$NOTES"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
